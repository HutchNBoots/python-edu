---
title: "Conditions — Easy"
instructions: "Fill in the two missing numbers so the code correctly prints `hot` when temperature is above 25, `warm` when it is between 15 and 25, and `cold` otherwise. The variable is already set to 30."
starterCode: |
  temperature = 30

  if temperature > ???:
      print("hot")
  elif temperature > ???:
      print("warm")
  else:
      print("cold")
expectedOutput: "hot"
xpReward: 50
---
